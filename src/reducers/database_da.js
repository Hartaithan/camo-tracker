import database from "../data/db_da_camo.json"

const daDatabase = (state = database, action) => {
    switch (action.type) {
        default: 
            return state;
    }
};

export default daDatabase;
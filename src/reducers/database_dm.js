import database from "../data/db_dm_camo.json";

const dmDatabase = (state = database, action) => {
    switch (action.type) {
        default: 
            return state;
    }
};

export default dmDatabase;